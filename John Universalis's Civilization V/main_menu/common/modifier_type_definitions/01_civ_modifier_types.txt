# Modifier types
# color = good(default)/neutral/bad
# boolean = no(default)/yes
# percent = no(default)/yes
# already_percent = no(default)/yes
# decimals = 2(default)/x (int)
# in game_data = {}
# 	category = all(default)/none/country/location/province/character/unit/mercenary/religion/internationalorganizatiuon/rebel/dynasty
#	min = -infinity(default)/x (fixed point)
#	max = +infinity(default)/x (fixed point)
#	cap_zero_to_one = no(default)/yes
#	scale_with_pop = no(default)/yes
# 	format = ""(default)/FormatPopCaps/FormatManPower/FormatGold
# 	ai = no(default)/yes
# 	bias_type = opinion/trust/voting (can have more than 1 entry - default=none of them)
# 	should_show_in_modifiers_tab = yes(default)/no (should modifier appear in the government modifiers tab)

is_city_state = { #used to prevent city states from colonizing
	boolean=yes
	color=bad
	game_data={
		category=country
	}
}

is_civ = { #used to make civs colonize
	boolean=yes
	color=good
	game_data={
		category=country
	}
}

is_cultural_cs = { #used to prevent city states from colonizing
	boolean=yes
	color=bad
	game_data={
		category=country
	}
}

is_industrial_cs = { #used to prevent city states from colonizing
	boolean=yes
	color=bad
	game_data={
		category=country
	}
}

is_militaristic_cs = { #used to prevent city states from colonizing
	boolean=yes
	color=bad
	game_data={
		category=country
	}
}

is_religious_cs = { #used to prevent city states from colonizing
	boolean=yes
	color=bad
	game_data={
		category=country
	}
}

is_scientific_cs = { #used to prevent city states from colonizing
	boolean=yes
	color=bad
	game_data={
		category=country
	}
}

is_trade_cs = { #used to prevent city states from colonizing
	boolean=yes
	color=bad
	game_data={
		category=country
	}
}

is_maritime_cs = { #used to prevent city states from colonizing
	boolean=yes
	color=bad
	game_data={
		category=country
	}
}